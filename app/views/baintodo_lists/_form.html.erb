<%= form_with(model: baintodo_list, local: true) do |form| %>
  <% if baintodo_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(baintodo_list.errors.count, "error") %> prohibited this baintodo_list from being saved:</h2>

      <ul>
        <% baintodo_list.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :descrption %>
    <%= form.text_area :descrption %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
